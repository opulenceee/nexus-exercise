# Runtime dependencies (exact versions are frozen after the first successful
# build for reproducibility — see README "Dependencies").
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<2.0
psycopg[binary]>=3.2,<4.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
